{
  "name": "dereferrer",
  "private": true,
  "scripts": {
    "start": "npm --prefix ../../ run start:tools",
    "build": "npm --prefix ../../ run build:tools",
    "build:tools": "npm run build"
  }
}
