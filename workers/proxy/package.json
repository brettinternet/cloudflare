{
  "name": "proxy",
  "private": true,
  "scripts": {
    "start": "npm --prefix ../../ run start:proxy",
    "build": "npm --prefix ../../ run build:proxy",
    "build:proxy": "npm run build"
  }
}
