{
  "name": "dereferrer",
  "private": true,
  "scripts": {
    "start": "npm --prefix ../../ run start:combined",
    "build": "npm --prefix ../../ run build:combined",
    "build:combined": "npm run build"
  }
}
